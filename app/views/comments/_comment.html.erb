
        <ul class="commentList">
            <li>
                <div class="commenterImage">
                  <%= image_tag avatar_url(comment.user) %>
                </div>
                <div class="commentText">
                    <p class=""><%= comment.body_html %></p> <span class="date sub-text">by: <%= comment.user.name %> on <%= comment.created_at.strftime("%d %b. %Y") %></span>
                </div>
            </li>
        </ul>